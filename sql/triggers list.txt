Number of total purchases per package.
(package_id, numPurchases)

trigger: after purchase of a package numPurchases++


 Number of total purchases per package and validity period.
(package_id, period, numPurchases)

trigger: after purchase of a package with that valPeriod numPurchases++

 Total value of sales per package with and without the optional products.
(package_id, totalWithOpt, totalWithoutOpt)

trigger: after purchase of a package update the totalWithoutOpt
trigger: after purchase of a package check from the optProduct and update totalWithOpt


 Average number of optional products sold together with each service package.
(package_id, numOptTotal, numOptAVG, numSales)

trigger: after purchase of package count the optional products, numSales++ and update AVG

 List of insolvent users, suspended orders and alerts.
Insolvent users
(user_id)
trigger: After update on users if insolvent true add
trigger: After update on users if insolvent false remove

Suspended orders
(order_id)
trigger: After update on orders if suspended true add
trigger: After update on orders if suspended false remove

Alerts
(alert_id, username,  daterejection, amount)
trigger: After update on alert update this

 Best seller optional product, i.e. the optional product with the greatest value of sales across all the sold service packages.
(opt_prod_id, salesVal)
trigger: after update on order(status=valid) fetch the optional orders and update their totalSalesValue
trigger: after update on opt order value check if > current best and eventually update it



TRIGGERS TO DO
Giova: 4 - 6 
Carlo: 1 - 2 - 3 - 5 






